# Copy to .env to override. compose.yaml falls back to these same values.
CLICKHOUSE_USER=liquibase
CLICKHOUSE_PASSWORD=liquibase

# Point at a registry copy of the CLI image instead of a locally built one.
# LIQUIBASE_IMAGE=ghcr.io/leonidgrishenkov/liquibase-clickhouse:4.33.0-1
